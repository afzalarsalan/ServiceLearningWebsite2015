@HtmlImport('fifth_page.html')
library fifthpage;
import 'package:polymer/polymer.dart';

/**
 * A Polymer fifth-page element.
 */
@CustomTag('fifth-page')
class FifthPage extends PolymerElement {

  /// Constructor used to create instance of FifthPage.
  FifthPage.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of fifth-page is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of fifth-page is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// fifth-page is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when fifth-page has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
