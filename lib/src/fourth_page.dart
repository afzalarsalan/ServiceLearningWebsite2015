@HtmlImport('fourth_page.html')
library fourthpage;
import 'package:polymer/polymer.dart';

/**
 * A Polymer fourth-page element.
 */
@CustomTag('fourth-page')
class FourthPage extends PolymerElement {

  /// Constructor used to create instance of FourthPage.
  FourthPage.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of fourth-page is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of fourth-page is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// fourth-page is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when fourth-page has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
